#!/bin/bash
#
# Autostart file for X server 

# Set correct gamma
case `hostname` in
  vix60)
    xgamma -gamma 1.25 &
    ;;
esac

# rxvt-unicode daemon
urxvtd -q -f -o &

# set wallpaper with feh
feh --bg-center ~/.wallpaper

