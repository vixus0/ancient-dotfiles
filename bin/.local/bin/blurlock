#!/usr/bin/env bash

export DISPLAY=:0

scr=$(mktemp).png
args="--nofork --ignore-empty-password --image=$scr"
blur=100

#scrot $scr
cp ~/.wallpaper.png $scr
gm mogrify -scale 5% -scale 2000% $scr
i3lock $args
